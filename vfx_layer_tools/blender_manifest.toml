schema_version = "1.0.0"

id = "vfx_layer_tools"
version = "2.2.1"
name = "VFX Layer Tools"
tagline = "VFX layer / scene / compositing manager"
maintainer = "VFX Pipeline"
type = "add-on"
tags = ["Compositing", "Render"]
license = ["SPDX:MIT"]

blender_version_min = "5.1.0"

[permissions]
files = "Render EXR sequences and manage compositing output files"
